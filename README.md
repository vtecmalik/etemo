# Etemo - Проверка корейской косметики

Мобильное приложение для проверки подлинности и безопасности корейской косметики по штрих-коду.

## Описание

Etemo - это React Native приложение (Expo), которое позволяет:

- 📷 Сканировать штрих-коды косметики камерой
- 🔍 Искать продукты по 13-значному штрих-коду (EAN-13)
- 💧 Проверять безопасность ингредиентов
- 📊 Просматривать детальную информацию о продуктах
- 📋 Сохранять историю поиска локально
- ❤️ Добавлять продукты в избранное (требует авторизации)
- ✨ Получать персональные рекомендации по типу кожи (требует авторизации)

## Технологии

- **React Native** с **Expo SDK 54**
- **TypeScript** для типобезопасности
- **React Navigation** (Stack + Bottom Tabs)
- **Expo Camera** для сканирования штрих-кодов
- **Supabase** для авторизации и хранения данных пользователей
- **AsyncStorage** для локального хранения истории
- **Expo Image** для оптимизации изображений
- **React Native SVG** для иконок

## Архитектура

```
etemo/
├── App.tsx                    # Точка входа
├── index.ts                   # Регистрация компонента
├── src/
│   ├── navigation/            # Навигация приложения
│   │   ├── MainNavigator.tsx  # Stack + Tab навигаторы
│   │   └── types.ts           # Типы навигации
│   ├── screens/               # Экраны приложения
│   │   ├── FeedScreen.tsx         # Главная лента продуктов
│   │   ├── ScannerScreen.tsx      # Сканер штрих-кодов
│   │   ├── ReviewWriteScreen.tsx  # Написание отзывов (в разработке)
│   │   ├── FavoritesScreen.tsx    # Избранное
│   │   ├── ProductResultScreen.tsx # Детальная информация о продукте
│   │   ├── ProfileScreen.tsx       # Профиль пользователя
│   │   ├── LoginScreen.tsx         # Авторизация через Google
│   │   ├── QuestionnaireScreen.tsx # Анкета о типе кожи
│   │   ├── SearchScreen.tsx        # Поиск по штрих-коду
│   │   └── HistoryScreen.tsx       # История поиска (не используется в навигации)
│   ├── components/            # Переиспользуемые компоненты
│   │   ├── CustomTabBar.tsx           # Кастомная Tab Bar с анимацией
│   │   ├── TabBarIcons.tsx            # SVG иконки для Tab Bar
│   │   ├── TouchableScale.tsx         # Кнопки с тактильным откликом
│   │   ├── OptimizedImage.tsx         # Оптимизированные изображения
│   │   ├── IngredientsRingIndicator.tsx # Цветное кольцо-индикатор
│   │   ├── Skeleton.tsx               # Skeleton loaders
│   │   └── LoadingAnimation.tsx       # Анимация загрузки (не используется)
│   ├── services/              # Сервисы для работы с данными
│   │   ├── api.ts             # API клиент для бэкенда
│   │   ├── storage.ts         # AsyncStorage для локальных данных
│   │   └── supabase.ts        # Supabase клиент
│   ├── types/                 # TypeScript типы
│   │   └── product.ts         # Типы продуктов, ингредиентов и т.д.
│   ├── constants/             # Константы
│   │   ├── theme.ts           # Цвета, шрифты, отступы
│   │   ├── api.ts             # API endpoints и конфигурация
│   │   └── loadingImages.ts   # Изображения для анимации загрузки
│   └── assets/                # Ресурсы
│       ├── icons/             # SVG иконки
│       └── loading/           # Изображения для анимации загрузки
├── assets/                    # Expo assets (иконки, splash screen)
│   ├── icon.png
│   ├── adaptive-icon.png
│   ├── favicon.png
│   └── splash-icon.png
├── app.json                   # Expo конфигурация
├── eas.json                   # EAS Build конфигурация
├── package.json               # Зависимости
├── tsconfig.json              # TypeScript конфигурация
├── babel.config.js            # Babel конфигурация
└── .env                       # Переменные окружения (не в git)
```

## Установка и запуск

### Требования

- Node.js 18+ и npm
- Expo Go приложение на телефоне (для тестирования)
- Для iOS: macOS + Xcode (для нативной сборки)
- Для Android: Android Studio (для нативной сборки)

### Шаги установки

1. Клонируйте репозиторий и перейдите в директорию:
```bash
cd etemo
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте `.env` файл (если нет):
```bash
EXPO_PUBLIC_SUPABASE_URL=https://ssshmkcuxzondimckuut.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
```

4. Запустите сервер разработки:
```bash
npm start
```

5. Отсканируйте QR-код в приложении Expo Go или запустите на эмуляторе:
```bash
npm run android    # Android эмулятор
npm run ios        # iOS симулятор (только macOS)
```

## API Backend

Приложение использует тот же бэкенд что и веб-версия:

- **Base URL**: `https://oleigh-barcode-checker.vercel.app`
- **Endpoints**:
  - `POST /api/search` - Поиск продукта по штрих-коду
  - `GET /api/ingredients-status` - Статус готовности ингредиентов
  - `GET /api/popular-products` - Популярные продукты
  - `GET /api/recommended-products` - Рекомендованные продукты

### Формат запроса (POST /api/search)

```json
{
  "barcode": "8809432018285"
}
```

### Формат ответа (найден)

```json
{
  "found": true,
  "result": {
    "name_en": "Product Name",
    "name_ru": "Название продукта",
    "name_ko": "제품명",
    "brand": "Brand Name",
    "brand_ko": "브랜드명",
    "brand_logo": "https://...",
    "img_url": "https://...",
    "source": "oleigh_products",
    "country": "South Korea",
    "categories": [...],
    "ingredients": {
      "type": "regular",
      "count": 25,
      "ingredients": [...]
    }
  }
}
```

### Формат ответа (не найден, но бренд распознан)

```json
{
  "found": false,
  "error": "Продукт не найден",
  "brand": {
    "name_en": "Brand Name",
    "name_ko": "브랜드명",
    "logo_url": "https://..."
  }
}
```

## База данных (Supabase)

Приложение использует Supabase для:

1. **Авторизация пользователей** (Google OAuth)
2. **Таблица `cosme_users`** - профили пользователей:
   - `id` (UUID, PK)
   - `email` (text)
   - `gender` (text)
   - `age_range` (text)
   - `skin_type` (text)
   - `skin_tone` (text)
   - `is_pregnant` (boolean)
   - `created_at`, `updated_at` (timestamp)

3. **Таблица `oleigh_products`** - каталог продуктов (для Feed)
   - `barcode` (text, PK)
   - `name_ru`, `name_en` (text)
   - `brand_name_en` (text)
   - `img_url` (text, nullable)

## Локальное хранилище (AsyncStorage)

Приложение сохраняет локально:

- **История сканирований** (`@etemo_scan_history`) - последние 100 проверенных продуктов
- **Избранное** (`@etemo_favorites`) - понравившиеся продукты

## Основные функции

### 1. Сканирование штрих-кодов

- Использует `expo-camera` с `CameraView`
- Распознает только EAN-13 штрих-коды (13 цифр)
- Тактильная обратная связь при успешном скане
- Оверлей с рамкой и инструкциями

### 2. Проверка безопасности ингредиентов

- Цветное кольцо-индикатор вокруг изображения продукта
- 4 категории риска:
  - 🟢 Зеленый (1-2) - безопасный
  - 🟠 Оранжевый (3-6) - средний риск
  - 🔴 Красный (7-10) - высокий риск
  - ⚪ Серый (-) - не определен
- Модальное окно с полным списком ингредиентов
- Polling для асинхронной загрузки ингредиентов (если не готовы сразу)

### 3. Анимация загрузки

- При поиске продукта показывается анимация с быстрой сменой картинок косметики
- Минимум 5 секунд для UX, даже если API отвечает быстрее
- Плавный переход от анимации к изображению продукта

### 4. Персонализация

- Google OAuth авторизация через Supabase
- Анкета из 5 шагов (пол, возраст, тип кожи, тон кожи, беременность)
- Рекомендации по типу кожи (в разработке)

### 5. Кастомная Tab Bar

- 4 таба: Смотрю, Ищу, Пишу, Люблю
- Анимированная капсула для активного таба
- Позиционирование на основе 5-колоночной сетки
- SVG иконки из `react-native-svg`

## Дизайн система

### Цвета

```typescript
COLORS = {
  primary: '#131518',           // Основной черный
  primaryLight: 'rgba(19, 21, 24, 0.08)', // Светлый фон
  background: '#F5F5F8',        // Фон приложения
  white: '#FFFFFF',
  greyLight: '#f2f2f7',
  gray4: '#6f6f6f',
  red: '#eb3d48',               // Акцент для ошибок
  riskSafe: '#22c55e',          // Безопасный
  riskMedium: '#f97316',        // Средний риск
  riskHigh: '#ef4444',          // Высокий риск
  riskUnknown: '#9ca3af',       // Не определен
}
```

### Отступы

```typescript
SPACING = {
  xs: 4, sm: 8, md: 12, lg: 16,
  xl: 20, xxl: 24, xxxl: 32
}
```

### Скругления

```typescript
BORDER_RADIUS = {
  sm: 8, md: 12, lg: 14, xl: 24, full: 9999
}
```

## Сборка

### EAS Build (рекомендуется)

```bash
# Установите EAS CLI
npm install -g eas-cli

# Войдите в аккаунт Expo
eas login

# Создайте сборку для Android
eas build --platform android

# Создайте сборку для iOS
eas build --platform ios
```

### Локальная сборка

```bash
# Android (требует Android Studio)
npm run android

# iOS (требует Xcode, только macOS)
npm run ios
```

## Тестирование

На данный момент в проекте нет автоматизированных тестов.

Для ручного тестирования:

1. Откройте приложение в Expo Go
2. Проверьте каждый экран и функцию
3. Проверьте сканирование штрих-кода
4. Проверьте поиск по известному штрих-коду (например, 8809432018285)
5. Проверьте авторизацию и анкету

## TODO / Будущие улучшения

### Высокий приоритет
- [ ] Реализовать функцию написания отзывов (ReviewWriteScreen)
- [ ] Реализовать алгоритм персональных рекомендаций
- [ ] Добавить возможность делиться продуктами
- [ ] Реализовать push-уведомления
- [ ] Добавить темную тему

### Средний приоритет
- [ ] Добавить фильтры и сортировку в Feed
- [ ] Реализовать поиск по названию/бренду
- [ ] Добавить кэширование изображений
- [ ] Оптимизировать FlatList с большими данными
- [ ] Добавить offline режим

### Низкий приоритет
- [ ] Добавить unit и e2e тесты
- [ ] Добавить аналитику (Amplitude/Mixpanel)
- [ ] Реализовать социальные функции (подписки, лайки)
- [ ] Добавить мультиязычность (EN, KO)

## Известные проблемы

1. **Сканер не работает на iOS симуляторе** - нужно реальное устройство
2. **OAuth redirect не работает в Expo Go** - нужна development build
3. **Изображения могут грузиться медленно** - нет кэширования на уровне API

## Структура API ответа для ингредиентов

### Regular тип (обычный продукт)

```json
{
  "type": "regular",
  "count": 25,
  "ingredients": [
    {
      "position": 1,
      "name_en": "Water",
      "name_ru": "Вода",
      "risk_score": "1",
      "tags": ["solvent"]
    }
  ]
}
```

### Set тип (набор продуктов)

```json
{
  "type": "set",
  "count": 3,
  "parts": [
    {
      "name": "Part 1",
      "name_en": "Cream",
      "name_ru": "Крем",
      "ingredients": [...]
    }
  ]
}
```

## Лицензия

Proprietary - Все права защищены

## Контакты

Для вопросов и предложений: [ваш email или ссылка на issues]

---

**Статус проекта**: В активной разработке 🚧

**Последнее обновление**: Декабрь 2024
